import {BRANDS,CATEGORIES} from '@/lib/data';export default function Sitemap(){} export async function getServerSideProps({res}){const urls=['/','/om','/kontakt','/vilkar','/privatliv','/cookies',...CATEGORIES.map(c=>`/kategori/${c.id}`),...BRANDS.map(b=>`/brand/${b.id}`)];const xml='<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+urls.map(u=>`<url><loc>https://pandarabatkoder.dk${u}</loc></url>`).join('')+'</urlset>';res.setHeader('Content-Type','application/xml');res.write(xml);res.end();return{props:{}}}